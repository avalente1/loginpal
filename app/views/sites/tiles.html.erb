<div class= "row-fluid">
  <div class= "span10 offset1">
    <table class="table table-hover table-condensed">
      <thead>
        <tr>
          <td id="td_company">Website</th>
          <td id="td_username">Username</th>
          <td id="td_pwhint">Password hint</th>
          <td id="td_edit"> </th>
          <td id="td_delete"> </th>
        </tr>
      </thead>
    </table>
    <%= render 'form_new', :site => Site.new %>

    <div class="btn-group">
      <% @sites.each do |site| %>
      <%#= link_to "#{image_tag(site.favicon) site.company}", site.site %>
      TODO Working here now
      <button class="btn">
        <%= image_tag site.favicon %>
        <%= site.company %>
      </button>
      <button class="btn dropdown-toggle" data-toggle="dropdown">
        <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
        <li>
          <%= link_to "Username: #{site.username_sb.decrypt ENV['SB_DECRYPT']}", site.site %>
        </li>
        <li>
          <%= link_to "Hint: #{site.pwhint_sb.decrypt ENV['SB_DECRYPT']}", "#" %>
        </li>
      </ul>
      <% end %>
    </div>


  </div>
</div>
